TO RUN GAME: open "main.py"

Python is required and Pygame, Pandas and Matplotlib modules are also required to run the game!
Modules can be installed by running "installmodules.bat" or by manually opening a command prompt and inputting following commands:
pip install pygame
pip install pandas
pip install matplotlib